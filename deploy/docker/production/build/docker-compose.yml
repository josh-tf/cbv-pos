# https://github.com/josh-tf/cbvpos  - Josh Bowden 2019-05-03
# Docker-Compose file - using ver 2 for compatibility
# build the production image (called via Travis CI)

version: '2'

services:
    cbv-pos:
        build:
            context: ./deploy/docker/production/build/
            dockerfile: Dockerfile
        container_name: cbv-pos